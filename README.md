# interactive-ansible-container
It creates a docker container from Ubuntu with ansible, then launches a container with that image - with the current directory mounted on `/app`.

This is especailly useful on windows, where ansible isn't fully supported.

## Steps
- build.ps1
- run.ps1
